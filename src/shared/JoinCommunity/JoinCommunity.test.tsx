import React from 'react';
import ReactDOM from 'react-dom';
import JoinCommunity from './JoinCommunity';

it('renders without crashing', () => {
  const div = document.createElement('div');
  ReactDOM.render(<JoinCommunity />, div);
});
